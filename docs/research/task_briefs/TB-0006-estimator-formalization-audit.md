# TASK_BRIEF

- **Identifier:** TB-0006 — Exact estimator formalization and equivalence audit
- **Status:** Completed as a formal method-claim kill; Gate 0 remains open
- **Date and owner:** 2026-08-29; Ultra scientific challenge with Research and
  statistical review, under the Commander's continuing instruction to advance
  the single project
- **Evidence gate:** Gate 0 literature, mathematical, implementation-identity,
  and protocol analysis only; no scientific execution
- **Primary outcome:** Decide whether one exact conditional-conflict estimator
  formula/interface can be defended prospectively against analytic equivalence
  to occupied disagreement, uncertainty-adjusted compatibility, evidential,
  and deterministic density-ratio scores. Freeze it as a recommendation only
  if it passes; otherwise record a method-claim kill or explicit unresolved
  blocker rather than manufacturing novelty.
- **Authoritative inputs:** `AGENTS.md`, `CODEX_TASK_GOVERNANCE.md`, the complete
  canonical record under `docs/research/`, DR-0009, TB-0005, primary
  peer-reviewed methods papers, and official implementation/licence/model
  documentation.
- **Allowed actions:** Read-only primary-literature and official-documentation
  research; symbolic derivation and deterministic arithmetic using no real or
  record-level data; independent read-only scientific/statistical/governance
  challenge; create `docs/research/estimator_formalization_audit.md`; update
  only `README.md`, `docs/research/README.md`,
  `docs/research/measurement_protocol.md`,
  `docs/research/statistical_analysis_plan.md`,
  `docs/research/baselines_and_ablations.md`,
  `docs/research/literature_matrix.md`,
  `docs/research/gate0_decision_dossier.md`,
  `docs/research/gate0_closure_audit.md`,
  `docs/research/decision_log.md`, `scripts/check_repository.py`,
  `tests/test_repository_contract.py`, and this brief; run local checks; make a
  normal non-force Git synchronization after all reviews pass.
- **Required questions:** For no more than three candidates, state the exact
  inputs, formula, trainable quantities, objective, calibration/normalization,
  inference interface, ambiguity/information-loss behavior, identifiability
  assumptions, and deterministic matched comparator. Derive every binary-case
  collapse and monotone equivalence that can be shown analytically. Check the
  closest primary prior art and exact official implementation/licence status.
  Select one candidate only if it has a non-cosmetic technical distinction and
  a falsifiable advantage unavailable to the matched deterministic route.
- **Forbidden actions:** Any dataset, record, report, image, checkpoint, model,
  weight, credential, agreement, access request, code repository clone,
  download, clinician contact, annotation, clinical editing, implementation,
  training, inference, tuning, experiment, hosted API, paid compute, new
  scientific route, Gate-0 closure, or claim of novelty, feasibility, clinical
  value, venue acceptance, or publication.
- **Exact boundary:** Only the named files may change. Public sources may be
  read but not downloaded into the repository. A mathematical distinction is
  not empirical validity; an absence from the audited sources is not proof of
  novelty. Facts, derivations, assumptions, recommendations, decisions, and
  kill consequences remain separate.
- **Required evidence and checks:** Direct primary/official links with transfer
  limits; explicit derivations; finite method decision; matched comparator and
  ablation mapping; cross-document staging reconciliation; independent
  scientific, statistical, and governance reviews; `pytest -q`;
  `python scripts/check_repository.py`; `git diff --check`; exact changed-path
  and sensitive-content review.
- **Promotion criteria:** Exactly one primary estimator identity is specified
  without outcome selection; its mathematical novelty is not merely a rename,
  monotone transform, or extra parameterization of an occupied score; its
  information and capacity budgets can be matched; and every unsupported claim
  is narrowed. If no candidate passes, the audit records that kill result.
- **Stopping criteria:** Stop selection if all candidates collapse analytically,
  require unavailable labels/information, cannot be matched fairly, lack an
  auditable implementation/licence route, or move the project outside the
  determinate-conflict construct.
- **External boundary:** Read-only public research and the final normal Git
  synchronization are the only external actions permitted.
- **Permitted claim after completion:** The repository contains an exact
  estimator recommendation or a documented formalization kill. It does not
  establish novelty, empirical identifiability, performance, Gate-0 closure,
  venue readiness, acceptance, or publication.

## Completion Record

- **Outcome:** Select none of the three pointwise candidates. Binary learned-
  belief/Gaussian excess disagreement reduces to a deterministic mean score
  after the required self-spread correction; conditional density ratio is a
  prior-adjusted deterministic classifier logit; evidential confident
  disagreement is an occupied RCML/Discounted-Belief-Fusion form.
- **Retained estimator:** The exact finite-sample plug-in estimator for
  `psi_mag` is retained as an estimator of the population specificity
  functional for an already frozen score. It is generally downward biased at
  finite sample size and is not a deployable pair-level semantic-conflict
  estimator.
- **Finite decision:** `G0-METHOD A` recommends a dated framework-centered
  claim amendment with one explicitly non-novel instrument and matched
  comparator. `G0-METHOD B` requires a new pre-data theory candidate. Neither
  option is owner-approved.
- **Implementation audit:** Five finite roles were specified with distinct
  supervision tags. Only ProbVLM versus POINT is same-information; DET-LR and
  DBF are privileged ceilings. Direct DBF vendoring and CLIP-Adapter code from
  the audited snapshot, which exposes no explicit licence file, are excluded;
  remaining implementation/licence choices are Gate-0 blockers.
- **Independent reviews:** Final scientific and statistical re-reviews passed
  the three formal kills, reader/model distinction, componentwise max-`t`
  inference, and nonlinear-link guardrail. Governance/licensing review passed
  this documentation audit. Gate-0 execution remains blocked pending owner
  decisions on method identity, supervision, provenance, and implementation.
- **External/actions boundary:** Only primary/official public pages were read.
  No dataset, record, model, checkpoint, repository clone, download, code
  implementation, experiment, clinical contact, or annotation occurred.
