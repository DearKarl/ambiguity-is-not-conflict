# TASK_BRIEF

- **Identifier:** TB-0011 — Simulation output/metric registry and semantic-
  count ledger
- **Status:** In progress; Gate 0 remains open
- **Date and owner:** 2026-08-29; Ultra scientific challenge with independent
  scientific, statistical, and governance review, under the Commander's
  continuing instruction to advance the single project
- **Evidence gate:** Repository-only Gate-0 protocol compilation; no project
  random stream, scientific implementation, benchmark, simulation, or clinical
  execution
- **Primary outcome:** Produce a complete logical output/metric registry and a
  deterministic, hardware-neutral semantic operation-count ledger for the
  unchanged TB-0009 manifests. Every output receives a unique identity, type,
  applicability/state rule, persistence/reconstruction rule, aggregate rule,
  and finite storage occurrence formula; every workload receives an exact,
  lower-bound, upper-bound, alternative-specific, or explicitly unresolved
  count without inferring runtime or feasibility.
- **Authoritative inputs:** `AGENTS.md`, `CODEX_TASK_GOVERNANCE.md`, the complete
  canonical record under `docs/research/`, DR-0011--DR-0013, and TB-0008--
  TB-0010.
- **Allowed actions:** Read the repository; create
  `docs/research/simulation_output_and_operation_registry.md`,
  `scripts/compile_simulation_semantic_count_ledger.py`,
  `reports/tables/simulation_metric_registry.csv`,
  `reports/tables/simulation_operation_registry.csv`,
  `reports/tables/simulation_semantic_count_ledger_summary.csv`, and this brief;
  update only `README.md`, `docs/research/README.md`,
  `docs/research/noncore_simulation_computational_design.md`,
  `docs/research/simulation_resource_feasibility_audit.md`,
  `docs/research/execution_budget_and_backbone_audit.md`,
  `docs/research/gate0_decision_dossier.md`,
  `docs/research/gate0_closure_audit.md`,
  `docs/research/decision_log.md`, `reports/tables/README.md`,
  `scripts/check_repository.py`, and `tests/test_repository_contract.py`;
  import the frozen TB-0009 enumerator; enumerate static symbols, formulas, and
  integer counts in memory; print a full per-cell ledger to standard output for
  hashing/review; track only the small registries and aggregate summary; run
  local deterministic compilation/checks; obtain independent read-only
  scientific, statistical, and governance reviews; and perform a final normal
  non-force Git synchronization after every review passes.
- **Required questions:** Freeze as recommendations only (1) which values are
  calculated, persisted, or reconstructed; (2) exact payload types and
  `VALUE/INAPPLICABLE/SCIENTIFIC_UNDEFINED/NOT_REACHED` semantics; (3) event,
  failure-precedence/component-mask, retry, and aggregate rules; (4) complete
  reliability and `MV-1` static/per-outer/per-cell/family fields; (5) exact
  output occurrence/byte formulas and extension counts; (6) exact or bounded
  raw-word, conversion, probability, calibration, bootstrap, FE/LOO, order-
  selection, exact-binomial, and serialization work units; (7) separate
  replay/materialization/cache alternatives; and (8) every unresolved
  scientific choice or implementation-dependent quantity that blocks closure.
- **Forbidden actions:** Modifying the verified TB-0009 enumerator or existing
  manifest CSV; instantiating any project RNG/HMAC stream or permutation;
  generating a synthetic buffer/observation; evaluating any DGP, calibration,
  bootstrap, reliability statistic, kappa, hierarchy, FE/LOO analysis,
  exact-binomial interval, or scientific metric; implementing a scientific
  serializer/checkpoint/restart path; creating or running a benchmark harness,
  timing, profiling, hardware/load test, or capacity estimate; selecting a
  deadline, retry/service level, hardware, allocation, `G0-RESOURCES` option,
  metric from outcomes, new diagnostic family, threshold, cell, or pruning
  rule; accessing data, readers, models, checkpoints, accounts, DUAs, clinical
  examples, paid/hosted compute, or downloads; Gate-0 closure; or promising
  venue acceptance or publication.
- **Exact compiler boundary:** The compiler must use only the Python standard
  library and frozen manifest metadata. It may create canonical strings,
  integer/formula rows, hashes, and byte-reproducible CSV. It may not call a
  random generator, HMAC, numerical/special-function library, scientific
  statistic, serializer for scientific records, or system profiler. A full
  per-cell ledger may be streamed to stdout but is not tracked. Registry and
  operation codes are protocol identifiers, not executable algorithms.
- **Required evidence and checks:** Explicit facts/inferences/assumptions/
  recommendations; unique registry and operation codes; typed state semantics;
  deterministic failure precedence plus component masks; exact applicability
  and occurrence formulas; complete aggregate/family rules; alternative-
  specific workload rows; exact/lower/upper/unresolved labels; TB-0009 count
  and hash reconciliation; byte-reproducible tracked tables; independent hand
  reconciliation; exact changed-path and sensitive-content review; independent
  reviews; `pytest -q`, `python scripts/check_repository.py`,
  `git diff --check`, and regeneration equality for all three tracked CSVs.
- **Promotion criteria:** Every currently required simulated output has a code,
  type, state/applicability rule, aggregate/reconstruction rule, and finite
  storage occurrence or explicit stop; every semantic workload maps exactly
  once to an exact/bounded/alternative/unresolved row; no observed-reader-only
  diagnostic is silently multiplied across outer simulation; all hashes/counts
  reconcile; and unresolved scientific choices are finite owner decisions, not
  zero-cost assumptions.
- **Stopping criteria:** Stop rather than guess if any truth reference,
  numerator/denominator, repeat or ambiguity domain, trace depth, failure
  precedence, undefined state, applicability, metric family, numerical domain,
  or count lacks canonical support. Stop if a cheaper path is assembled from
  incompatible alternatives, a bound is relabelled exact, a scientific output
  is dropped to reduce storage, or any work/runtime/capacity claim exceeds
  static protocol compilation.
- **External boundary:** No external action is authorized except final normal
  Git synchronization. If unavailable, preserve the reviewed local commit and
  report exact divergence; never force-push or rewrite history.
- **Permitted claim after completion:** The repository contains complete
  static freeze candidates for the simulation output registry and semantic-
  count ledger, with unresolved choices explicitly blocking later benchmark or
  resource qualification. It does not establish owner approval, scientific-
  implementation correctness, runtime, capacity, allocation, feasibility,
  operating performance, reader reliability, `MV-1` validity, Gate-0 closure,
  acceptance, or publication.

## Completion Record

Pending independent review and verification.
