# Configurations

**Status:** Reserved until Gate 0 closes

Each future experiment cell should receive an immutable, human-readable
configuration recording dataset and split revision, finding task, intervention
cell, model/checkpoint revision, estimator, calibration method, seed, hardware,
and output directory. Confirmatory configurations must be frozen before final
evaluation.
